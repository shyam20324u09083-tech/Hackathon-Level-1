HTML structure for a student tracker
Copilot Chat
HTML structure for a student tracker
You said: <!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8" />
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>StudyTrack — Student Activity Tracker</title>
  <link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Syne:wght@400;600;800&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg: #0d0f14;
      --surface: #161921;
      --card: #1e2230;
      --border: #2a2f45;
      --accent: #7DF9AA;
      --accent2: #5bc8ff;
      --warn: #ff7e5f;
      --text: #e8eaf0;
      --muted: #7a8099;
      --radius: 14px;
    }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: 'Syne', sans-serif;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* Background grid */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(rgba(125,249,170,0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(125,249,170,0.03) 1px, transparent 1px);
      background-size: 40px 40px;
      pointer-events: none;
      z-index: 0;
    }

    /* NAV */
    nav {
      position: sticky;
      top: 0;
      z-index: 100;
      background: rgba(13,15,20,0.85);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 2rem;
      height: 64px;
    }

    .logo {
      font-family: 'Space Mono', monospace;
      font-size: 1.1rem;
      color: var(--accent);
      letter-spacing: -0.03em;
    }

    .logo span { color: var(--accent2); }

    .nav-tabs {
      display: flex;
      gap: 4px;
    }

    .nav-tab {
      background: transparent;
      border: none;
      color: var(--muted);
      font-family: 'Syne', sans-serif;
      font-size: 0.85rem;
      font-weight: 600;
      padding: 6px 16px;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.2s;
    }

    .nav-tab:hover { color: var(--text); background: var(--border); }
    .nav-tab.active { color: var(--accent); background: rgba(125,249,170,0.1); }

    /* PAGES */
    .page { display: none; animation: fadeIn 0.3s ease; }
    .page.active { display: block; }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* HERO */
    .hero {
      position: relative;
      z-index: 1;
      padding: 5rem 2rem 3rem;
      max-width: 800px;
      margin: 0 auto;
      text-align: center;
    }

    .hero-badge {
      display: inline-block;
      font-family: 'Space Mono', monospace;
      font-size: 0.7rem;
      color: var(--accent);
      border: 1px solid rgba(125,249,170,0.3);
      padding: 4px 12px;
      border-radius: 100px;
      margin-bottom: 1.5rem;
      letter-spacing: 0.1em;
    }

    .hero h1 {
      font-size: clamp(2.4rem, 6vw, 4rem);
      font-weight: 800;
      line-height: 1.05;
      letter-spacing: -0.03em;
      margin-bottom: 1rem;
    }

    .hero h1 em {
      font-style: normal;
      color: var(--accent);
    }

    .hero p {
      font-size: 1rem;
      color: var(--muted);
      line-height: 1.7;
      max-width: 520px;
      margin: 0 auto 2rem;
    }

    .btn-primary {
      background: var(--accent);
      color: #0d0f14;
      border: none;
      font-family: 'Syne', sans-serif;
      font-size: 0.9rem;
      font-weight: 700;
      padding: 12px 28px;
      border-radius: 10px;
      cursor: pointer;
      transition: all 0.2s;
      letter-spacing: 0.02em;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 24px rgba(125,249,170,0.25);
    }

    /* STATS BANNER */
    .stats-banner {
      position: relative;
      z-index: 1;
      display: flex;
      gap: 1px;
      max-width: 800px;
      margin: 2rem auto 0;
      background: var(--border);
      border-radius: var(--radius);
      overflow: hidden;
    }

    .stat-item {
      flex: 1;
      background: var(--card);
      padding: 1.5rem;
      text-align: center;
    }

    .stat-num {
      font-family: 'Space Mono', monospace;
      font-size: 2rem;
      font-weight: 700;
      color: var(--accent);
    }

    .stat-label {
      font-size: 0.75rem;
      color: var(--muted);
      margin-top: 4px;
      letter-spacing: 0.05em;
      text-transform: uppercase;
    }

    /* PROGRESS BAR */
    .progress-section {
      position: relative;
      z-index: 1;
      max-width: 800px;
      margin: 2rem auto;
      padding: 0 2rem;
    }

    .progress-label {
      display: flex;
      justify-content: space-between;
      font-size: 0.8rem;
      color: var(--muted);
      margin-bottom: 8px;
      font-family: 'Space Mono', monospace;
    }

    .progress-track {
      height: 8px;
      background: var(--border);
      border-radius: 100px;
      overflow: hidden;
    }

    .progress-fill {
      height: 100%;
      background: linear-gradient(90deg, var(--accent), var(--accent2));
      border-radius: 100px;
      transition: width 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
    }

    /* ACTIVITIES PAGE */
    .activities-section {
      position: relative;
      z-index: 1;
      max-width: 800px;
      margin: 0 auto;
      padding: 2rem;
    }

    .section-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 1.5rem;
    }

    .section-title {
      font-size: 1.4rem;
      font-weight: 800;
      letter-spacing: -0.02em;
    }

    .filter-btns {
      display: flex;
      gap: 6px;
    }

    .filter-btn {
      background: var(--card);
      border: 1px solid var(--border);
      color: var(--muted);
      font-family: 'Syne', sans-serif;
      font-size: 0.75rem;
      font-weight: 600;
      padding: 5px 12px;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.2s;
    }

    .filter-btn:hover, .filter-btn.active {
      color: var(--accent);
      border-color: rgba(125,249,170,0.4);
      background: rgba(125,249,170,0.07);
    }

    /* ACTIVITY CARDS */
    .activities-grid {
      display: grid;
      gap: 12px;
    }

    .activity-card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 1.25rem 1.5rem;
      display: flex;
      align-items: center;
      gap: 1rem;
      transition: all 0.25s;
      cursor: pointer;
      position: relative;
      overflow: hidden;
    }

    .activity-card::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: 3px;
      background: var(--border);
      transition: background 0.25s;
    }

    .activity-card:hover {
      border-color: rgba(125,249,170,0.2);
      transform: translateX(4px);
    }

    .activity-card:hover::before { background: var(--accent); }
    .activity-card.completed::before { background: var(--accent); }
    .activity-card.completed { opacity: 0.7; }

    .activity-icon {
      width: 44px;
      height: 44px;
      border-radius: 10px;
      background: var(--surface);
      border: 1px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.3rem;
      flex-shrink: 0;
    }

    .activity-info { flex: 1; min-width: 0; }

    .activity-name {
      font-size: 0.95rem;
      font-weight: 600;
      margin-bottom: 3px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .activity-card.completed .activity-name {
      text-decoration: line-through;
      color: var(--muted);
    }

    .activity-desc {
      font-size: 0.78rem;
      color: var(--muted);
      line-height: 1.4;
    }

    .activity-tag {
      font-family: 'Space Mono', monospace;
      font-size: 0.65rem;
      padding: 3px 8px;
      border-radius: 6px;
      flex-shrink: 0;
    }

    .tag-pending {
      background: rgba(255,126,95,0.12);
      color: var(--warn);
      border: 1px solid rgba(255,126,95,0.25);
    }

    .tag-completed {
      background: rgba(125,249,170,0.1);
      color: var(--accent);
      border: 1px solid rgba(125,249,170,0.25);
    }

    .complete-btn {
      background: transparent;
      border: 1px solid var(--border);
      color: var(--muted);
      font-family: 'Space Mono', monospace;
      font-size: 0.7rem;
      padding: 6px 12px;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.2s;
      white-space: nowrap;
      flex-shrink: 0;
    }

    .complete-btn:hover {
      background: rgba(125,249,170,0.1);
      color: var(--accent);
      border-color: rgba(125,249,170,0.4);
    }

    .complete-btn.done {
      color: var(--accent);
      border-color: rgba(125,249,170,0.3);
    }

    /* PROGRESS PAGE */
    .progress-page {
      position: relative;
      z-index: 1;
      max-width: 800px;
      margin: 0 auto;
      padding: 2rem;
    }

    .progress-cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 12px;
      margin-bottom: 2rem;
    }

    .progress-card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 1.5rem;
    }

    .progress-card h3 {
      font-size: 0.75rem;
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: 0.08em;
      margin-bottom: 0.75rem;
      font-family: 'Space Mono', monospace;
    }

    .big-num {
      font-family: 'Space Mono', monospace;
      font-size: 2.5rem;
      font-weight: 700;
      line-height: 1;
    }

    .big-num.green { color: var(--accent); }
    .big-num.blue { color: var(--accent2); }
    .big-num.orange { color: var(--warn); }

    .donut-wrap {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem 0;
    }

    .donut {
      width: 140px;
      height: 140px;
      transform: rotate(-90deg);
    }

    .donut-bg { fill: none; stroke: var(--border); stroke-width: 12; }
    .donut-fill {
      fill: none;
      stroke: var(--accent);
      stroke-width: 12;
      stroke-linecap: round;
      stroke-dasharray: 352;
      stroke-dashoffset: 352;
      transition: stroke-dashoffset 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
    }

    .donut-label {
      position: absolute;
      font-family: 'Space Mono', monospace;
      font-size: 1.4rem;
      font-weight: 700;
      color: var(--text);
    }

    .donut-container {
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .category-list { display: grid; gap: 10px; }

    .category-row {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 10px;
      padding: 0.9rem 1.2rem;
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .cat-icon { font-size: 1.1rem; width: 28px; text-align: center; }
    .cat-name { flex: 1; font-size: 0.85rem; font-weight: 600; }
    .cat-bar-wrap { width: 120px; }
    .cat-bar-track { height: 5px; background: var(--border); border-radius: 100px; overflow: hidden; }
    .cat-bar-fill { height: 100%; background: var(--accent); border-radius: 100px; transition: width 0.6s ease; }
    .cat-count { font-family: 'Space Mono', monospace; font-size: 0.7rem; color: var(--muted); width: 40px; text-align: right; }

    /* TOAST */
    .toast {
      position: fixed;
      bottom: 2rem;
      right: 2rem;
      background: var(--card);
      border: 1px solid var(--accent);
      color: var(--accent);
      font-family: 'Space Mono', monospace;
      font-size: 0.78rem;
      padding: 10px 18px;
      border-radius: 10px;
      z-index: 999;
      transform: translateY(80px);
      opacity: 0;
      transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
      pointer-events: none;
    }

    .toast.show { transform: translateY(0); opacity: 1; }

    /* RESPONSIVE */
    @media (max-width: 600px) {
      nav { padding: 0 1rem; }
      .hero { padding: 3rem 1rem 2rem; }
      .activities-section, .progress-page { padding: 1rem; }
      .section-header { flex-direction: column; align-items: flex-start; gap: 0.75rem; }
      .activity-card { flex-wrap: wrap; }
      .stats-banner { flex-direction: column; gap: 1px; }
      .logo span:last-child { display: none; }
    }
  </style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="logo">Study<span>Track</span></div>
  <div class="nav-tabs">
    <button class="nav-tab active" onclick="showPage('home')">Home</button>
    <button class="nav-tab" onclick="showPage('activities')">Activities</button>
    <button class="nav-tab" onclick="showPage('progress')">Progress</button>
  </div>
</nav>

<!-- HOME PAGE -->
<div id="page-home" class="page active">
  <div class="hero">
    <div class="hero-badge">✦ HACKATHON LEVEL-1</div>
    <h1>Track Your<br/><em>Learning Journey</em></h1>
    <p>Stay on top of your daily assignments, activities, and progress. Mark tasks as complete, monitor your streak, and visualize your growth.</p>
    <button class="btn-primary" onclick="showPage('activities')">View Activities →</button>
  </div>

  <div class="stats-banner" id="home-stats">
    <div class="stat-item">
      <div class="stat-num" id="stat-total">0</div>
      <div class="stat-label">Total Tasks</div>
    </div>
    <div class="stat-item">
      <div class="stat-num" id="stat-done">0</div>
      <div class="stat-label">Completed</div>
    </div>
    <div class="stat-item">
      <div class="stat-num" id="stat-pending">0</div>
      <div class="stat-label">Pending</div>
    </div>
    <div class="stat-item">
      <div class="stat-num" id="stat-pct">0%</div>
      <div class="stat-label">Progress</div>
    </div>
  </div>

  <div class="progress-section">
    <div class="progress-label">
      <span id="prog-text">Overall Progress</span>
      <span id="prog-pct">0%</span>
    </div>
    <div class="progress-track">
      <div class="progress-fill" id="home-bar" style="width:0%"></div>
    </div>
  </div>
</div>

<!-- ACTIVITIES PAGE -->
<div id="page-activities" class="page">
  <div class="activities-section">
    <div class="section-header">
      <div class="section-title">All Activities</div>
      <div class="filter-btns">
        <button class="filter-btn active" onclick="filterCards('all', this)">All</button>
        <button class="filter-btn" onclick="filterCards('pending', this)">Pending</button>
        <button class="filter-btn" onclick="filterCards('completed', this)">Completed</button>
      </div>
    </div>
    <div class="activities-grid" id="activities-grid"></div>
  </div>
</div>

<!-- PROGRESS PAGE -->
<div id="page-progress" class="page">
  <div class="progress-page">
    <div class="section-header">
      <div class="section-title">Your Progress</div>
    </div>

    <div class="progress-cards">
      <div class="progress-card">
        <h3>Completed</h3>
        <div class="big-num green" id="p-done">0</div>
      </div>
      <div class="progress-card">
        <h3>Pending</h3>
        <div class="big-num orange" id="p-pending">0</div>
      </div>
      <div class="progress-card">
        <h3>Total</h3>
        <div class="big-num blue" id="p-total">0</div>
      </div>
      <div class="progress-card" style="display:flex; flex-direction:column; align-items:center;">
        <h3>Completion Rate</h3>
        <div class="donut-container">
          <svg class="donut" viewBox="0 0 120 120">
            <circle class="donut-bg" cx="60" cy="60" r="48"/>
            <circle class="donut-fill" id="donut-circle" cx="60" cy="60" r="48"/>
          </svg>
          <span class="donut-label" id="donut-label">0%</span>
        </div>
      </div>
    </div>

    <div class="section-header">
      <div class="section-title" style="font-size:1rem;">By Category</div>
    </div>
    <div class="category-list" id="category-list"></div>
  </div>
</div>

<!-- TOAST -->
<div class="toast" id="toast"></div>

<script>
  const activities = [
    { id: 1, name: "HTML Basics", desc: "Learn HTML tags, structure, and semantic elements.", icon: "🌐", category: "Web Dev", completed: false },
    { id: 2, name: "CSS Styling", desc: "Master selectors, box model, flexbox, and grid layouts.", icon: "🎨", category: "Web Dev", completed: false },
    { id: 3, name: "JavaScript Fundamentals", desc: "Variables, loops, functions, and DOM manipulation.", icon: "⚡", category: "Programming", completed: false },
    { id: 4, name: "Responsive Design", desc: "Make web pages look great on all screen sizes.", icon: "📱", category: "Web Dev", completed: false },
    { id: 5, name: "Git & Version Control", desc: "Commit, branch, merge, and collaborate with Git.", icon: "🔀", category: "Tools", completed: false },
    { id: 6, name: "React Introduction", desc: "Components, props, state, and the virtual DOM.", icon: "⚛️", category: "Framework", completed: false },
    { id: 7, name: "API Integration", desc: "Fetch data from REST APIs using JavaScript.", icon: "🔌", category: "Programming", completed: false },
    { id: 8, name: "Project: Portfolio Site", desc: "Build and deploy your personal portfolio website.", icon: "🚀", category: "Project", completed: false },
  ];

  // Load from localStorage
  const saved = localStorage.getItem('studytrack-activities');
  if (saved) {
    const savedArr = JSON.parse(saved);
    savedArr.forEach(s => {
      const a = activities.find(x => x.id === s.id);
      if (a) a.completed = s.completed;
    });
  }

  function save() {
    localStorage.setItem('studytrack-activities', JSON.stringify(activities.map(a => ({ id: a.id, completed: a.completed }))));
  }

  function getStats() {
    const total = activities.length;
    const done = activities.filter(a => a.completed).length;
    return { total, done, pending: total - done, pct: Math.round((done / total) * 100) };
  }

  function updateStats() {
    const { total, done, pending, pct } = getStats();
    document.getElementById('stat-total').textContent = total;
    document.getElementById('stat-done').textContent = done;
    document.getElementById('stat-pending').textContent = pending;
    document.getElementById('stat-pct').textContent = pct + '%';
    document.getElementById('home-bar').style.width = pct + '%';
    document.getElementById('prog-pct').textContent = pct + '%';
    document.getElementById('prog-text').textContent = `${done} out of ${total} activities completed`;

    // Progress page
    document.getElementById('p-done').textContent = done;
    document.getElementById('p-pending').textContent = pending;
    document.getElementById('p-total').textContent = total;
    document.getElementById('donut-label').textContent = pct + '%';
    const circumference = 2 * Math.PI * 48;
    const offset = circumference - (pct / 100) * circumference;
    document.getElementById('donut-circle').style.strokeDashoffset = offset;
    document.getElementById('donut-circle').style.strokeDasharray = circumference;

    // Category breakdown
    const cats = {};
    activities.forEach(a => {
      if (!cats[a.category]) cats[a.category] = { total: 0, done: 0, icon: a.icon };
      cats[a.category].total++;
      if (a.completed) cats[a.category].done++;
    });

    const catList = document.getElementById('category-list');
    catList.innerHTML = Object.entries(cats).map(([name, c]) => {
      const cp = Math.round((c.done / c.total) * 100);
      return `<div class="category-row">
        <div class="cat-icon">${c.icon}</div>
        <div class="cat-name">${name}</div>
        <div class="cat-bar-wrap">
          <div class="cat-bar-track"><div class="cat
